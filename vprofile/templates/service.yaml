apiVersion: v1
kind: Service
metadata:
  name: {{ include "vprofile.fullname" . }}
  labels:
    {{- include "vprofile.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vprofile.selectorLabels" . | nindent 4 }}
